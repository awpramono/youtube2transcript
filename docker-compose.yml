services:
  youtube2transcript:
    build: .
    image: youtube2transcript:latest
    container_name: youtube2transcript
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      - PORT=${PORT:-3000}
      - NODE_ENV=production
